# 배포 구성 (시험장 실행 기준)

시험장에서 실제로 도는 구성이다. 2026-09-16 기준. 다른 곳과 어긋나면 **이 문서와 `results/BASELINE.json` 이 맞다.**

실행 파일은 `_kisa_port/tools/kisa_items.py` **하나뿐이다.** 항목마다 다른 파일을 쓰지 않는다.

```
python _kisa_port/tools/kisa_items.py --item <fire|intrusion|loitering|falldown> --rtsp <주소> --out <폴더>
```

---

## 1. 항목별 구성과 실측

| 항목 | 점수 | 가중치 | 입력 | 판정 |
| :-- | --: | :-- | :-- | :-- |
| 방화 | **100.00** | `fire_fog.pt` @640 + `fire_small.pt` @960 (앙상블) | 6분할 타일, **낱장 추론** | 불 ≥0.40 이 20스텝(10초) 창에 3회 · 지연 10초 |
| 쓰러짐 | **100.00** | `yolo11x-pose.pt` @1280 | 0.1초 간격 | SeqNet 문턱 0.755 · 연속 4창 |
| 배회 | **96.55** | `person_v2.pt` | BoT-SORT 전체프레임 @640 | 구역 체류 6초 · 끊김 6 · 확정대기 5초 · **늦은 일행(20초/3명/방금도착)** · 지연 10초 |
| 침입 | **94.74** | `person_v3.pt` | 3x3 타일 @960, **부분검출 억제 끔** | 연속 2표본 · 확정대기 · 끊김 허용 2 |

실측 로그와 정검·미검·오검 수는 `results/BASELINE.json` 에 있다. 점수를 묻는 질문은 그 파일 하나만 본다.

---

## 2. 판정 규칙 본문

숫자만 보면 왜 그 숫자인지 모른다. 실제로 무엇을 세고 언제 터지는지를 적는다.
코드는 `_kisa_port/tools/kisa_items.py` 의 `FireJudge` / `FallJudge` / `IntrusionRule` / `LoiterRule` 이다.

### 공통

- 판정 시각(onset) 을 구한 뒤 **SA = onset + 지연**. 지연은 방화 10초, 배회 10초, 침입 0초, 쓰러짐 0초.
- 채점: 정답 시각 기준 `[-2초, +10초]` 안에 든 SA 하나만 정검.
- **창을 벗어난 SA 는 두 번 깎인다.** 오검 1 + 그 정답은 미검 1.
- `F1 = 2rp/(r+p) x 100`, 90 이상 합격.
- 주의: 실행 로그의 클립별 `오검` 표시는 그 클립의 모양만 적은 것이다. 합계는 위 규칙대로 오검·미검을 둘 다 센다.

### 방화 (FireJudge)

1. 0.5초마다 프레임 하나. 전체 + 4분할 + 중앙 = **6뷰**.
2. 6뷰 x 가중치 2벌 = 표본당 **12회 낱장 추론**. 그 표본의 값 = 12회 중 불 최고 신뢰도.
3. 불 >= **0.40** 이면 그 표본은 적중. **연기는 보지 않는다** (`smoke=1.1` 은 신뢰도가 넘을 수 없는 값이라 조건이 항상 거짓).
4. 최근 **20표본(10초)** 창에 적중이 **3회** 모이면, 그 창의 첫 적중 시각이 onset.
5. 클립별 분기는 없다. 모든 편이 같은 세 숫자(0.40 / 20 / 3) 를 쓴다.

### 쓰러짐 (FallJudge)

1. 0.1초마다 자세 추정 @1280, 신뢰도 상위 5명.
2. 중심 거리로 트랙 연결(0.8초까지 끊겨도 같은 사람).
3. 0.5초마다 트랙별 59차원 피처 하나를 쌓고, 최근 **20슬롯(10초)** 을 SeqNet 에 넣어 확률을 얻는다.
4. 확률 >= **0.755** 가 **연속 4창**이면 그 첫 창 시각이 onset. 여러 사람이면 가장 이른 사람.
5. 트랙이 끊긴 뒤에도 0벡터를 계속 이어 붙인다. 쓰러진 뒤 검출이 끊기는 패턴이 바로 여기서 터진다.

### 침입 (IntrusionRule)

1. 0.5초마다 3x3 타일 @960 사람 검출. 타일 NMS 의 **부분검출 억제는 끈다**. 트래커가 ID 를 붙인다.
2. 사람 상자 네 꼭짓점 중 **3개**가 구역 안이면 진입.
3. 같은 ID 가 **2표본 연속** 진입이면 그 사람의 진입 시각을 적는다. 끊김은 **2표본**까지 봐준다.
4. 가장 늦은 진입 시각을 들고 있다가 **24초** 동안 새 진입이 없으면 확정.
5. onset = **마지막으로 들어온 사람의 진입 시각** (확정 시각이 아니다).

### 배회 (LoiterRule)

1. 0.5초마다 전체 프레임 @640 사람 검출 + BoT-SORT ID.
2. **발끝**(상자 하단)이 구역 안이면 체류 누적. 끊김은 **6표본**까지 봐준다.
3. 체류가 **6초**를 채우면 배회자. 그 사람의 진입 시각을 적는다.
4. 마지막 배회자가 생긴 뒤 **5초** 기다려 확정. onset = 마지막 배회자의 진입 시각.
5. 다만 아래 **넷을 모두** 만족하면 5초를 넘겨 온 사람도 "마지막 사람" 으로 받는다(늦은 일행).
   - 지금 고른 사람이 **아직 구역 안** (1초 안에 목격)
   - 앞 배회자와 **20초 이내**
   - 구역 인원 **3명 이하**
   - 그 사람이 **방금 도착** (진입 시각 = 구역에 처음 들어온 시각)

## 3. 쉽게 틀리는 곳

| 항목 | 이렇게 하면 틀린다 | 무슨 일이 나나 |
| :-- | :-- | :-- |
| 방화 | 6뷰를 한 배치로 추론 | 크기가 다른 타일이 공통 크기로 패딩돼 값이 달라진다(0.545 대 0.519). 안개편이 정검 대 오검으로 갈린다 |
| 방화 | 연기 채널을 켠다 | 88.89 -> 63.16. 안개편은 연기가 0초부터 높다. `smoke=1.1` 은 "절대 만족 안 함" 이라는 뜻 |
| 침입 | 타일 NMS 의 부분검출 억제를 켠다 | 94.74 -> 87.72. 제출 경로는 `contain=None`(끔) |
| 침입 | 타일 입력을 1280 으로 올린다 | 94.74 -> 84.21. 학습 해상도(960)와 어긋난다 |
| 배회 | 타일 검출 덤프로 규칙을 고른다 | 제출은 BoT-SORT 전체프레임이다. 덤프가 다르면 68.97 |
| 쓰러짐 | 자세 해상도를 640 으로 내린다 | 먼 사람 관절이 흔들려 트랙이 잘게 끊긴다 |

## 4. 덤프 (규칙을 고를 때 읽는 것)

**살아 있는 것은 이것뿐이다.** 나머지는 `dumps/_archive/2026-09-16/` 에 내렸다.

| 항목 | 덤프 | 만든 방식 | 재현 |
| :-- | :-- | :-- | --: |
| 침입 | `dumps/intrusion_tile_v3` | `person_redump.py --contain 2.0` | 94.74 = 실측 |
| 배회 | `dumps/loiter_botsort_v2` | `server_tdump.py --conf 0.20` (BoT-SORT 640) | 96.55 = 실측 |
| 방화 | `dumps/score_tl/<실험>.json` | `score_kisa.py --tiles --imgsz <학습 해상도>` | 전용 도구로 대조 |
| 쓰러짐 | `dumps/fall_seq_1280` | 자세 1280 확률 덤프 | 전용 도구로 대조 |

## 5. 손댄 뒤 반드시 돌리는 것

| 명령 | 무엇을 보나 |
| :-- | :-- |
| `python scripts/check_repro.py` | 덤프 재계산이 `BASELINE.json` 실측과 같은가 |
| `python scripts/fire_weight_check.py <실험> <가중치.pt> [해상도]` | 그 덤프를 만든 가중치가 맞는가 |
| `MROD_PW=... python scripts/check_thor.py` | Thor 배포본 8개가 서버와 같은가 (작업 PC 에서) |
| `python scripts/check_models.py` | 운영 가중치가 전부 `results/MODELS.json` 에 적혀 있는가 |
| `python scripts/check_layout.py` | 산출물이 규약 자리에 있는가 |
| `python scripts/audit_stale.py` | 판정 중복·죽은 경로·낡은 산출물·미참조 스크립트 |

### 판단할 때 쓰는 도구

점수 하나만 보고 정하지 않는다. 아래를 같이 본다.

| 명령 | 무엇을 보나 | 언제 |
| :-- | :-- | :-- |
| `python scripts/fire_ens_try.py` | 가중치 조합별 방화 점수·못 잡는 편·창 여유가 아슬한 편 | 새 방화 가중치가 나올 때마다 |
| `python scripts/fragile.py` | **표본 하나를 빼면 판정이 깨지는 편**(취약성) | 방화 규칙·가중치를 바꿀 때마다 |
| `python scripts/early_late.py` | SA 가 정답보다 이른가 늦은가 · 창 여유 | 조합을 바꿔 조기 경보가 늘었는지 볼 때 |
| `python scripts/conf_sweep.py` | 침입 신뢰도 문턱 전수 스윕(고원인지 첨점인지) | 문턱을 만지고 싶을 때 |
| `python scripts/data_worth.py` | 덤프 전부를 배포 규칙으로 재채점 + 데이터 구성 대조 | 어떤 원본을 넣고 뺄지 정할 때 |
| `python scripts/label_state.py` | 손라벨·전파 현황과 학습셋 반영 여부 | 라벨을 추가한 뒤 |
| `python scripts/leak_check.py` | **채점셋 라벨이 학습셋에 섞였는가** | 학습셋을 빌드한 뒤 반드시 |
| `python scripts/sam_state.py` | SAM2 전파 산출물 현황 | 전파를 돌린 뒤 |
| `python scripts/gt_check.py [클립...]` | 원본 GT 시각이 실제 영상과 맞는가 | GT 가 수상할 때 |
| `python scripts/show_fire.py <클립> <시각...>` | 그 시각에 무엇을 불로 봤는지 상자로 | 왜 잡았나/못 잡았나 볼 때 |
| `python scripts/cancel.py` | 대기 중인 실험 취소 표시(도는 학습은 안 건드림) | 큐 순서를 바꿀 때 |
| `python scripts/zone_conf.py <가중치>` | **미검 편의 영역 안 최고 신뢰도**. 손라벨 효과를 F1 대신 이걸로 본다 | 사람 가중치가 나올 때마다 |

일회성 탐색 스크립트는 `scripts/_archive/<날짜>/` 에 있다. 결론은 전부 문서로 옮겼다.

## 6. 알아 둘 것

- `fire_small.pt` 의 원본은 `runs/` 에 없다. 2026-09-15 재학습 실패(`train_failed`, epoch 3)가 그 자리를 덮어썼고,
  쓸 수 있는 것은 `logs/_archive/2026-09-15/r960오염실험/` 사본뿐이다. `fire_weight_check.py` 로 덤프 재현을 확인했다.
  지금 도는 `f960_best_s` / `f1280_best_s` 가 눈편을 잡으면 이 의존이 없어진다.
- 방화 채점셋은 10편이라 한 편이 1.6점이다. LOOCV 80.00(낙관 +20). `C00_089_0001` 과 `C00_195_0001` 은 창 여유가 0.5초다.
  **100 을 실력으로 읽으면 안 된다.** 안전 구간은 90~94.74.
- 배회는 30편 · LOOCV 96.55 · 낙관 0.00 이라 네 항목 중 가장 단단하다.

## 7. 남은 위험 (2026-09-16 기준)

| # | 무엇 | 왜 위험한가 | 확인 방법 |
| :-- | :-- | :-- | :-- |
| 1 | ~~방화 앙상블 속도를 안 쟀다~~ **쟀다. 여유 4.68배** | Thor 실측 106.8ms / 주기 500ms. 최악 115.5ms 도 들어온다 | 끝 (`docs/kisa/KISA_시험_체크리스트.md`) |
| 2 | **쓰러짐 1280 이 네 항목 중 가장 빠듯하다. 여유 1.72배** | Thor 실측 58.2ms / 주기 100ms(최악 63.7ms). 지금은 들어오지만 여기에 계산을 더 얹으면 제일 먼저 깨진다 | 해상도·모델을 더 올릴 때마다 `_kisa_port/tools/bench_thor.py` 재측정 |
| 3 | **방화가 연기를 안 본다** | `smoke=1.1` = 연기 조건 영구 거짓. 불꽃이 안 보이고 연기만 나는 편이 나오면 전편 미검 | 사전시험 영상 구성 확인. 2026-09-16 사용자 확인: 연기만 나는 편은 없을 것으로 봄 |
| 4 | **방화 100.00 의 근거가 얇다** | 10편뿐, LOOCV 80.00, 창 여유 0.5초가 2편. 안전 구간은 90~94.74 | 편수 늘리기 |
| 5 | **침입과 배회가 다른 경로다** | 같은 "구역 안 사람" 인데 가중치도(v3/v2) 해상도도(960타일/640전체) 추적기도 다르다. 두 벌을 따로 유지해야 한다 | 한쪽으로 합쳤을 때 점수 확인 |
| 6 | **배회 해상도 640 을 안 올려봤다** | 쓰러짐은 640 -> 1280 으로 84.21 -> 94.74 였다. 배회는 아직 640. 남은 미검 2편이 여기일 수 있다 | `track_imgsz` 960 으로 재덤프 |
| 7 | ~~침입에 늦은 일행 규칙이 없다~~ **아님. 검출이 안 되는 것** | 미검 3편은 전부 `sa=None`(아무도 진입으로 안 잡힘)이다. 구역 안 최고 신뢰도 0.19/0.18/0.41 < 문턱 0.45. 특히 C00_255_0001 은 사람이 300px 로 또렷한데 두 가중치 모두 conf 0.05 에서 0개다(야간 IR). 문턱을 낮추면 30편 전체가 나빠진다 | 끝. 다음은 야간 IR 사람 학습 (`docs/EXPERIMENTS.md` 4.11) |
| 8 | **인증용 편수는 배포용의 10배다** | 지금 파라미터는 배포용 10/30/30/10편에 맞춘 값이다. 인증용 100/300/300/100편은 비공개다 | 사전시험 결과 보고 확정 후 설정 잠금 |

### 이미 확인된 것 (다시 하지 말 것)

**RTSP 실시간 경로는 두 번 실측했다.** 자세한 내용은 `docs/kisa/KISA_시험_체크리스트.md` §3.1 · §3.2 에 있다.

| 언제 | 무엇 | 결과 |
| :-- | :-- | :-- |
| 2026-09-09 | VLC 2.1.5 드라이런 | TCP 거부·절대 pts·UDP 타임아웃 경계·재시도 시간, 4건 찾아 고침 |
| 2026-09-15 | 사내 PC ↔ Thor 직결 | `lap` 누락·`app.vision` 임포트 즉사·PyAV 조용한 폴백·가짜 PTS 역전, 4건 찾아 고침 |
| 2026-09-15 | 배회 3편 RTSP | SA 181 / 200 / 199초 = **오프라인과 초 단위 완전 일치**, 정검 3 |
| 2026-09-15 | Thor 항목별 여유(추론만) | 표는 순수 추론값이라 배포 경로와 다르다. 섞어 읽지 말 것 |
| 2026-09-16 | Thor 배포 경로 실측 | 방화 4.68배 · 침입 4.83배 · **쓰러짐 1.72배** · 배회 17.80배. 네 항목 모두 최악값까지 주기 안 |

**B200 처리 시간을 실시간 지표로 읽지 말 것.** 배포 성능은 **Thor 수치**가 기준이다.

## 8. 2026-09-16 밤에 돌려 둔 것과 다음에 할 일

### 지금 돌고 있는 것 (아침에 확인)

| 잡 | 진행 | 예상 종료 | 무엇을 위한 것인가 |
| :-- | :-- | :-- | :-- |
| `f960_best_s_20260915` | 36/60 | 약 6.8시간 | **`fire_small.pt` 대체재.** 그 파일은 원본이 `runs/` 에 없고 아카이브 사본뿐이다(6장) |
| `p1280_coco_kisa_s_20260915` | 13/60 | 약 11.4시간 | 1280 은 근거가 약하다. 끝나도 배포 후보로 보지 말 것 |
| 박스 백필 | 110/397 | 약 10시간 | 검수 화면용. 점수와 무관 |

**대기 4개는 취소했다**(`results/<이름>/score.txt` 에 사유 기록).
`s2_m960`(모델 키울 이유 없음) · `p1280_hand`(해상도) · `p960_coco` · `p960_hand`(순서 바꾸려고 내림).

### 아침에 먼저 할 것

```bash
python scripts/fire_ens_try.py      # f960 이 들어간 조합이 배포보다 나은가
python scripts/fragile.py           # 그 조합이 표본 하나 빼도 견디는가
```

**점수만 보지 말고 `fragile.py` 의 "깨지는 편" 을 같이 볼 것.** 이유는 아래.

### 방화 전략을 바꿔야 할 근거 (2026-09-16 실측)

| 구성 | 점수 | 표본 하나 빼면 깨지는 편 | 정검 중 이른 편 |
| :-- | --: | --: | --: |
| 앙상블 2벌 (지금 배포) | 100.00 | **1편** (`C00_195_0001`, 표본 3개 의존) | 1 |
| **`fire_small` 단독 @960** | **94.74** | **0편** | 1 |
| `fire_fog` 단독 @640 | 84.21 | 0편 | 0 |

앙상블 100점은 안개편 하나를 벼랑 끝에서 붙잡은 값이다.
**본시험은 편수가 10배라 이 취약성이 그대로 드러난다.** 90점을 방어하는 시험이지 100점을 받는 시험이 아니다.
→ `fire_small` 계열 단일 모델(94.74, 취약 0)로 가는 것을 우선 검토한다. `f960` 이 그 자리를 대신할 후보다.

참고: "앙상블이 조기 경보를 양산한다" 는 우리 데이터에서는 사실이 아니다.
정검 10편 중 이른 것은 1편뿐이고, 그 1편은 `fire_small` 단독에서도 이미 이르다(`scripts/early_late.py`).
SA = 검출 + 10초이고 KISA 정답도 발화 + 10초라, 우리 검출이 늦는 쪽이 기본값이다.

### 손라벨 가치 검증 (다음 실험 1순위)

절대 점수(F1)로 판단하면 안 된다. 손라벨만 학습한 모델은 배경을 모르므로 오검이 많아 40점대가 나온다.
**미검 3편의 "영역 안 최고 신뢰도" 가 올라가는지만 본다.**

| 편 | 조건 | 지금 최고 신뢰도 | 문턱 |
| :-- | :-- | --: | --: |
| `C00_249_0003` | 주간 설경, 원경 | 0.19 | 0.45 |
| `C00_275_0001` | 야간 안개, 원경 | 0.41 | 0.45 |
| `C00_255_0001` | **야간 IR, 300px 또렷** | **0.18** | 0.45 |

상자로 직접 보려면 `python scripts/show_fire.py <클립> <시각...>` 을 쓴다(방화용이지만 어느 시각에 무엇을 봤는지 확인하는 방식은 같다).

측정 도구를 만들어 뒀다. **F1 을 보지 말고 이것을 본다.**

```bash
python scripts/zone_conf.py runs/<실험>/yolo11s/weights/best.pt
```

가중치 하나를 받아 미검 3편의 GT 앞뒤 15초를 훑어 **영역 안 최고 신뢰도**와 그 시각을 찍는다.

`C00_255_0001` 이 핵심이다. 크기 문제가 아니라 **야간 IR 도메인을 못 본다**(conf 0.05 에서도 0개).
여기가 `0.18 → 0.5` 로 오르면 손라벨이 작동하는 것이다. F1 이 낮아도 상관없다. COCO 와 섞으면 해결된다.

#### 참고: 손라벨 단독 모델의 F1 은 낮게 나오는 게 정상이다

2026-09-16 에 손라벨만(1,189장) 학습한 사람 가중치를 배포 경로로 다시 채점했다.

```
[loitering] 정검 12 미검 18 오검 8 -> 48.00
```

오검 8편 중 3편은 **영상 시작 10초에 경보가 났다**(`sa=10.0`). 배경을 사람으로 오인한 것이다.
손라벨만으로는 배경을 배울 수 없으니 당연한 결과다. **이 점수로 손라벨을 판단하면 안 된다.**
COCO 와 섞으면 사라지는 종류의 오검이다.

**먼저 손라벨을 다시 빌드해야 한다.** 지금 `handset_*` 은 09-15 판이고 그 뒤 손라벨이 607박스 늘었다.

```bash
python scripts/label_state.py       # 손라벨·전파 현황
python scripts/sam_state.py         # SAM2 전파 산출물(채점셋 클립이 섞여 있으니 빌더 확인)
python scripts/leak_check.py        # 채점셋이 학습셋에 섞였는지 (지금은 깨끗)
python scripts/build_trainset.py person --no-gt --name handset_person_20260916
```

### 사람 항목의 기술 부채

배포 중인 `person_v3`(침입 94.74)·`person_v2`(배회 96.55)는 **재현이 불가능하다.**
학습 데이터의 57%가 의사라벨 18,532장인데 그 데이터셋은 삭제됐고 방침상 다시 쓸 수 없다.
의사라벨 없이 학습한 것들은 침입 91.23 / 배회 86.21 이 최고다.
**가중치 파일 자체를 잃으면 복구 방법이 없다.** `results/MODELS.json` 참조.

### 방화 데이터 다이어트 (시간이 남으면)

한 번 학습에 21.6만장 · 960 · 60에폭 = 약 15시간이다. 기여도를 재 보면 이렇다(`scripts/data_worth.py`).

| 데이터 | 크기 | 기여 |
| :-- | --: | :-- |
| `wildfire_pos_yolo` | 59,474 (27%) | **+11.8 (최대)** |
| `fasdd_snowfog` | 3,579 (2%) | +8.1 |
| 손라벨 하드네거티브 | 8,392 (4%) | +6.5 |
| `fasdd_yolo` | **95,314 (44%)** | +5.4 (1편 차이, 불확실) |
| `azimjaan_yolo` | 10,739 (5%) | **0** (두 번 확인) |
| `dfire_yolo` | 21,527 | **0** |
| AI허브 베이스 24k→48k | | **역효과** (66.67 → 57.14) |

- `azimjaan` 은 빼도 된다. 베이스는 25% 로 줄여도 점수가 같았다.
- **`fasdd_yolo`(전체의 44%)를 빼도 되는지는 아직 안 돌려봤다.** 빼면 21.6만 → 8.1만장으로 줄어 15시간이 6시간이 된다.
  다만 표의 +5.4 는 `wildfire_pos` 가 없던 시절 값이고 10편에서 1편 차이라 근거가 약하다. 한 번 확인할 값어치는 있다.
- **해상도 960 이 최적이다.** 640 은 88.89, 1280 은 77.78. 모델 크기(m/l/x)는 640 에서 전부 88.89 로 평평했다.
  `yolo11s@960`(94.74)이 `yolo11x@640`(88.89)보다 낫다.

### 10편짜리 채점의 한계 (판단할 때 반드시 감안)

방화는 1편이 약 5~6점이다. **12점 미만 차이는 2편이고 노이즈일 수 있다.**
위 표에서 믿을 만한 것은 `wildfire_pos`(+11.8, 여러 조합에서 재현)뿐이고
`fasdd`(+5.4)는 1편 차이라 단정할 수 없다.
