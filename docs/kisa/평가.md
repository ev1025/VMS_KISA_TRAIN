# KISA 지능형 CCTV 인증 평가 기준 정리 (ARGOS VMS, 방화·침입·배회·쓰러짐)

근거: **[안내서_원문발췌.md](안내서_원문발췌.md)** (PDF 원문을 쪽 번호와 함께 옮겨 둔 것),
`신청서/[MROD]지능형 CCTV 사전시험신청서_*.pdf` 4건, [KISA_시험_체크리스트.md](KISA_시험_체크리스트.md).

작성 2026-09-12, 원문 대조 2026-09-16. 사전시험 2026-09-28.

**여기 적는 것** 무엇으로 점수를 매기는가 (채점 규칙·이벤트 정의·시험 절차·제출물)
**여기 안 적는 것** 배포 구성([../DEPLOY.md](../DEPLOY.md)) · 미검 편 원인([../미검원인.md](../미검원인.md)) · 안내서 원문([안내서_원문발췌.md](안내서_원문발췌.md))
이 문서는 "무엇으로 점수를 매기는가" 와 "우리가 지금 몇 점인가" 를 다룬다.
**안내서에 뭐라고 쓰여 있는지는 [안내서_원문발췌.md](안내서_원문발췌.md) 를 본다**(PDF 는 레포에 없다).
시험장 절차·구현 대조·검증 기록은 [KISA_시험_체크리스트.md](KISA_시험_체크리스트.md).

---

## 1. 한눈에

| 항목 | 값 |
|---|---|
| 신청 분야 | 일반분야(C), RGB/IR(00). 방화·침입·배회·쓰러짐 4항목(복수 선택 가능) |
| 평가 방식 | **F1 스코어 방식**(4항목 전부). IoU 방식은 안전분야(드론 화재)·익수자·실종자만 해당 |
| 합격선 | 항목별 **90점 이상** (본시험, 인증용 DB 기준) |
| 채점 단위 | 클립 1편 = GT 이벤트 1건. 우리 SA 파일의 경보 1건과 비교 |
| 비교 대상 | 우리 PC가 자동 저장한 SA XML ↔ 진흥원 GT XML (파일명 동일, 확장자 xml) |
| 영상 | 1280×720, H.264, 10분 이내. 날씨(맑음·눈·비·안개)·시간 6종(일출·9·12·15시·일몰·야간)·거리 3종(근·중·원) |
| DB 3종 | 배포용(우리가 보유, 방화 10·침입 30·배회 30·쓰러짐 10편) / 시험용(사전시험, 진흥원 방문 시 RTSP) / 인증용(본시험, **비공개**, 서울에서만) |
| 구축 편수 | 안내서 표(RGB/IR): 배회 300 · 침입 300 · 방화 100 · 쓰러짐 100. **구축 편수이지 시험 편수가 아니다** |
| 인증서 | 유효기간 **3년**, 1회에 한해 2년 연장 |
| 비용 | 시험·인증 무료 |

---

## 2. 판정 정의 (F1 방식, 안내서 §3)

| 판정 | 조건 |
|---|---|
| 정상검출(TP) | SA 의 AlarmDescription 이 GT 와 일치 **그리고** SA 의 StartTime 이 항목별 "정상검출 시각" 창 안 |
| 미검출(FN) | GT 에 이벤트가 있는데 SA 에 이벤트 종류·시각을 기록하지 못함 |
| 오검출(FP) | SA 의 이벤트 종류가 GT 와 다름 **또는** StartTime 이 창 이전·이후 |

- 창을 벗어난 경보 = **오검출 + 미검출 동시**. 안내서 원문이 두 항목에 각각 걸린다.
  - 오검출: "SA 파일의 이벤트 시작 시각이 이벤트별 정상검출 시간 **이전, 이후**에 기록하는 경우"
  - 미검출: "GT에서 이벤트가 기록되어 있으나, SA 파일에서 이벤트 종류 및 이벤트 시작 시각을 **기록하지 못한** 경우"
  - 즉 창 밖 경보 하나는 FP 를 1 늘리고 그 GT 를 TP 로 못 채워 FN 도 1 늘린다. **늦거나 이른 경보가 무경보보다 나쁘다.**
- 정상검출 시각 창 = **[이벤트 시작시각 − 2초, 이벤트 시작시각 + 10초]** (4항목 공통).
- 사람 이외 차량·조류 등은 검출하면 안 된다(사람 항목 오검출).
- 항목 선택 시험이므로 신청 항목 외 이벤트를 SA 에 쓰면 오검출. 우리 SA 생성기는 `--item` 하나만 기록한다.

### 점수 산출식

```
검출률 r = TP / (TP + FN)
정밀도 p = TP / (TP + FP)
점수    = 2 × r × p / (r + p) × 100
```

안내서 예시(TP 90 · FN 10 · FP 30) → r 0.90, p 0.75 → **81.81점**.
한 편이 창 밖 경보가 되면 FN +1, FP +1 두 번 깎인다. 10편 항목에서 1편 미검출 = 94.74, 1편 창 밖 = 90.00, 2편 미검출 = 88.89.

---

## 3. 항목별 이벤트 시작시각 (안내서 표, 일반분야)

| 항목 | 정의 | 이벤트 시작시각(GT StartTime) | 특이사항 | SA Description |
|---|---|---|---|---|
| 방화 | 사람이 방화하여 연기 또는 화염이 발생 | **최초** 연기 또는 화염 발생 **+10초** | 최초 발생 1건만 SA 기록 | `FireDetection` |
| 침입 | 1명 또는 다수가 특정 영역에 진입 | 사람 **몸 전체**가 영역에 진입한 시각 | 다수면 **마지막 사람** 기준 | `Intrusion` |
| 배회 | 특정 영역에서 10초 이상 배회 | 몸 전체가 영역 진입 후 **10초 경과** 시각 | 다수면 **마지막 사람** 기준 | `Loitering` |
| 쓰러짐 | 사람이 쓰러진 상황 | 바닥에 **머리가 닿는** 시각 | 다수면 **처음** 쓰러지는 사람 기준. 싸움 영상의 쓰러짐은 SA 기록 금지 | `Falldown` |

- **방화 정의는 "연기 **또는** 화염" 이다.** 화염이 안 보이고 연기만 나는 편도 방화 이벤트다.
  지금 우리 구성은 연기 채널을 끄고(`smoke=1.1`) 불만 본다. 그런 편이 나오면 못 잡는다(`docs/DEPLOY.md` 위험 3번).
- Description 은 대소문자·스펠링 엄격(FAQ 8: "스펠링 오류, 띄어쓰기, 대소문자, 헤더값 누락"). `AlarmDuration` 은 채점에 쓰이지 않지만 형식은 맞춘다.
- 침입·배회의 "영역"은 영역파일(`C00_XXX.map`, 장소 코드별)로 주어진다. 쓰러짐·방화는 영역 없음.
- 파일명 규칙 `C00_장소3자리_영상4자리.mp4`. 같은 장소 코드는 같은 카메라·구역이다.

---

## 4. 시험 절차와 채점 시점

| 단계 | 내용 | 채점 |
|---|---|---|
| 배포용 DB | 신청서 제출 → 저장매체로 수령. 우리가 GT 포함해 보유 | 자체 채점(오프라인 채점기 `score_kisa`) |
| 1·2단계 | 시험신청서·보안서약서 제출, 사전시험 일자 확정(2026-09-28) | 없음 |
| 3단계 사전시험 | 우리 PC 지참 방문. 진흥원 PC 가 시험용 DB 를 RTSP 로 실시간 전송, 우리 PC 가 SA 자동 저장 → 제출 | 진흥원이 GT 와 비교해 **점수 통보** |
| 4단계 본시험 | 신청서·공문 제출 → PC 와 **SA 생성 매뉴얼** 제출. 진흥원 직원이 매뉴얼대로 조작, **인증용 DB**(비공개)로 평가 | 90점 이상이면 5단계 |
| 5단계 인증 | 인증서 발급 | |

- 사전시험은 점수만 받는 리허설이다. 본시험은 제출 후 수정 불가, 종료 후 저장매체 파기.
- 시험용·인증용은 실시간 RTSP 스트림이다. 오프라인 채점과 달리 되감기·재추론 불가, 클립 사이 스트림 단절(세부: 체크리스트 §2·§3.1).
- 배포용·시험용은 서울·부산, 인증용은 서울만.

---

## 5. 우리 자체 채점 방식과 공식 채점의 관계

| 지표 | 무엇 | 어디서 | 용도 |
|---|---|---|---|
| **채점 F1** | 배포용 DB 편별 SA ↔ GT, 위 §2 규칙 그대로 | `score_kisa`, 라이브 `kisa_items.py` | 공식 점수 예측. 편수가 10~30 이라 값이 계단식(10편: 66.67·82.35·88.89·94.74) |
| **채점셋 mAP** | 배포용 검증영상에서 우리가 손라벨·전파한 박스로 잰 검출 mAP50 (방화 771프레임, 사람 1,289프레임) | `eval_map.py` → `results/<실험>/eval_map.json` | 모델 간 검출력 비교. F1 보다 민감. **공식 채점과는 무관한 내부 지표** |
| 학습 검증 mAP | 학습 목록에서 뽑은 val | ultralytics 학습 로그 | 참고만. 과거 val_small 0.87 은 학습 데이터 누수였음 |

- 배포용 검증영상은 **학습에 넣지 않는다**(`use: eval`, 빌드 시 누수 검사). 배경 하드네거티브로도 쓰지 않는다. 라벨은 mAP 평가에만 쓴다.
- 규칙 파라미터(임계·연속 프레임·hold·gap)는 배포용 10~30편에 스윕한 값이라 과적합 위험이 있다. 스윕 곡선이 평평한 값만 채택한다(침입 gap 1~4 전 구간 동일 → 채택, 트랙 신뢰도 규칙은 단일 첨점 → 기각).

---

## 6. 현재 성적

**여기 적지 않는다.** 숫자는 `results/BASELINE.json`,
해석(무엇이 취약한가·미검 편의 원인·앞으로 할 일)은 **[../미검원인.md](../미검원인.md)**.

## 7. 제출물·주의

- 사전시험: PC 지참, 백신 설치, 인터넷 불가, 영역파일·목록파일 로컬 저장, SA 자동 저장 폴더. 절차는 체크리스트 §5.
- 본시험: PC + **SA 생성 매뉴얼**(진흥원 직원이 그대로 조작). 제출 후 수정 불가 → 임계·규칙은 사전시험 뒤 확정하고 설정 파일 잠금. 전용 SSD 반입(파기됨).
- 서류: 시험 신청서(항목별 4건), 보안서약서, 개인정보 동의서, 본시험 공문. `신청서/` 폴더.

### 모델 구조에 대한 제약은 없다 (2026-09-16 원문 확인)

안내서 전체에 **모델 개수·구조를 제한하는 문구가 없다.** 앙상블·다중 가중치·타일 추론 모두 걸리지 않는다.

- 제출물은 "지능형 CCTV 제품 - PC, 매뉴얼, 소프트웨어 등" 이다(별지 제2호 서식).
- 채점은 **SA XML 과 GT 의 비교뿐**이다(p.19). 내부 구현은 보지 않는다.
- FAQ 7: "운영체제는 별도로 지정되어 있지 않습니다. (…) **RTSP 통신, VLC 기반 영상재생 및
  정답지(SA) XML 파일이 생성되어야 합니다.**" → 요구사항은 이 셋뿐이다.

실제로 걸리는 것은 규정이 아니라 **실시간 처리**다. Thor 실측으로 네 항목 모두 주기 안에 들어온다
(방화 앙상블 12뷰 106.8ms / 주기 500ms, 여유 4.68배. 체크리스트의 Thor 표).

### 제품 수정 불가 시점

> 신청인이 제출한 제품은 **수정, 보완될 수 없으며**, K-ICTC 담당자의 요청에 의해 특정한 경우에만 조치할 수 있다. (p.9)

사전시험 제출본까지 묶이는지 본시험 제출본만인지는 **원문에 명시가 없다.** 진흥원에 확인이 필요하다.
보수적으로는 사전시험(2026-09-28) 전에 임계·규칙을 확정하고 설정 파일을 잠근다.
