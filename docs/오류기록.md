# 2026-09-16 하루에 낸 오류 넷

`CLAUDE.md` 의 `말하기 전에 돌릴 것` 표가 왜 생겼는지의 기록이다. 규칙이 아니라 사례다.

| 내가 말한 것 | 사실 | 어디 있었나 | 왜 틀렸나 |
| :-- | :-- | :-- | :-- |
| "RTSP 실측을 한 번도 안 했다" | 09-09 · 09-15 두 번 했다 | `docs/kisa/KISA_시험_체크리스트.md` 3.1 · 3.2 | 그 문서를 안 열고 B200 로그만 봤다. 그 로그는 학습과 GPU 를 나눠 쓴 값이라 실시간 지표가 아니다 |
| "안내서 PDF 가 없다" (여러 번) | 사용자가 여러 번 줬다 | 지금은 `docs/kisa/안내서_원문발췌.md` | 받을 때마다 읽고 답만 하고 레포에 안 넣었다 |
| "손라벨만으로는 배경을 못 배운다" | 하드네거티브가 이미 들어간다(1,396장 중 107장) | `docs/data.md` 3장 · `handset_*/meta.json` · 폴더 이름 `_hn` | 다른 모델이 준 설명을 우리 데이터로 확인하지 않고 받아썼다 |
| "mAP 검증셋도 낡았다" | 다시 빌드해도 771장 992박스로 같다 | — | 파일 수정 시각만 보고 단정했다. 오늘 라벨은 연구개발 영상 쪽이고 검증셋은 채점셋 라벨만 쓴다 |

네 건의 공통점

- 간접 증거로 단정했다. 파일 시각 · 다른 모델의 설명 · 관련 없는 로그.
- 직접 확인하면 1분이면 드러날 것들이었다. 문서 열기 · 다시 빌드해 비교하기 · 그려 보기.
- 자료는 다 있었다. 문서가 없어서가 아니라 안 찾아서 생겼다.

검사기도 같은 병을 앓았다

| 검사기 | 자가 시험 | 결과 |
| :-- | :-- | :-- |
| `audit_stale` 의 화면(js) 검사 | 했다. `core.js` 에서 `return row.sa` 를 빼서 세 항목이 다 잡히는 것을 봤다 | 맞게 동작 |
| `stale_scan` 의 신선도 검사 | 안 했다 | 틀린 답을 냈다(위 네 번째) |

검사기는 틀려도 티가 안 난다. 아무것도 안 잡으면 "깨끗하다" 로 보이고, 헛경보를 내면 사람이 무시하게 된다.
