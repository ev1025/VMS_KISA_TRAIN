# VMS: KISA 지능형 CCTV 인증 레포지토리 (CLAUDE.md)

> **분야:** 방화 · 침입 · 배회 · 쓰러짐

---

## 1. 대원칙 및 절대 금지 사항

**기본 원칙:** 레포지토리는 오직 `vms/` 단 하나입니다. 심링크나 중복 폴더를 절대 생성하지 마세요.

### 절대 금지 사항 (Do Not)
* **Train/Test 유출:** 채점셋(`data/원본데이터/kisa_배포_검증영상`)을 학습 데이터에 넣지 마세요.
* **비인가 폴더 생성:** `data/` 디렉토리 하위에 `원본데이터` 및 `학습데이터` 외의 다른 폴더를 만들지 마세요.
* **실행 중인 실험 변경:** 실행 중인 큐 스크립트나 yaml을 편집하여 실험을 추가하지 마세요. (바이트 오프셋이 깨져 고아 프로세스가 발생합니다)
* **규약 우회:** 문서에 적힌 규약을 코드에서 임의로 우회하지 마세요. (러너가 강제하는 필수 값입니다)

### 단일 기준 원칙 (갈라짐 방지)
같은 사실이 두 곳에 적혀 있으면 언젠가 갈라집니다. 항목마다 기준은 한 곳뿐입니다. 코드를 복사하지 말고 `import` 해서 사용하세요.
* **판정 규칙·실행 코드:** `_kisa_port/tools/kisa_items.py`
* **4항목 점수:** `results/BASELINE.json` (실측만 적습니다)
* **가중치 계보:** `results/MODELS.json`
* **배포 구성:** `docs/DEPLOY.md`
* **덤프:** 항목당 한 벌. 나머지는 `dumps/_archive/<날짜>/`
*(주의: 덤프는 파일로 남지만 가중치는 재학습에 덮어써집니다. 덤프만 보고 모델을 고르지 마세요.)*

---

## 2. 디렉토리 구조 및 산출물 위치

| 폴더 | 설명 및 내용 |
| :--- | :--- |
| `data/원본데이터/` | 수신한 원본 데이터 그대로 보관 (`<출처>_<내용>` 형식) |
| `data/학습데이터/` | YOLO 가공 데이터셋 + `손라벨/` (편집기가 사용하는 JSON 원천 데이터) |
| `configs/` | 실험 큐 yaml 및 `datasets.yaml` (데이터셋 규격 단일 기준) |
| `scripts/` | 현재 사용 중인 현역 스크립트 (과거 스크립트는 `scripts/_archive/<날짜>/`로 이동) |
| `model/` | 모델 가중치 (베이스 모델은 `pretrained/`, 직접 학습한 `.pt` 파일은 루트에 보관) |
| `runs/`, `results/`, `logs/queue/` | 학습 산출물, 점수 및 모델 계보, 실행 로그 |
| `dash_v2/` | 모니터링 대시보드 (포트 `8890`) |
| `dumps/` | 추론 트랙 덤프 파일 (`intrusion_tile`, `loiter_trk_id`, `fire_box` 등) |
| `_exp/` | 작업별 임시 목록 및 캐시 (삭제 가능) |

**실험 큐(`scripts/exp_queue.py`) 고정 산출물 위치**
* **학습 가중치:** `runs/<실험>/<모델>/weights/best.pt`
* **점수·설정:** `results/<실험>/{score.txt, meta.json, eval_map.json}`
* **실행 로그:** `logs/queue/<실험>.log`
* **목록·캐시(임시):** `_exp/<실험>/`

---

## 3. 명명 및 실험 규칙 (2026-09-17/18 확정)

### 실험 규칙
1. **평가 해상도 = 학습 해상도:** 1280으로 학습한 모델은 1280으로 잰다. (배포 해상도도 학습에 맞춰야 함 -> Thor 속도 선확인)
2. **학습은 한 번에 하나, GPU 전체 사용:** exp_queue는 `--jobs 1`. 모델이 작아 GPU가 남는 것이 실측된 경우에만 병렬 사용.
3. **손라벨 스냅샷 고정:** 라벨은 계속 늘어나므로 비교할 때는 같은 스냅샷끼리만 비교. (예: `handset_person_YYYYMMDD`)
4. **라벨은 장수가 아닌 '구성' 확인:** 늘어난 장수보다 '실패하는 조건이 늘었나'를 먼저 셈.

### 명명 규칙
* **학습셋:** `<항목>_<comp|mask>[_hn]_<날짜>`
  * `comp`: 사람이 눈으로 확인한 것만
  * `mask`: 확인분 + 미확인 전파까지 전부
  * `_hn`: 하드 네거티브 포함 시
  * *(예: `fire_comp_hn_20260918`)*
* **실험명:** `<f|p><해상도>_<comp|mask>[_hn]_x<오버샘플 배율>_<날짜>`
  * *(예: `f1280_comp_hn_x8_20260918`)*
* **사용 금지 단어:**
  * `hand`: 손라벨/완료 표시 중의적 의미라 사용 금지
  * `_s`: 현재 전부 yolo11s이므로 생략 (다른 모델 사용 시에만 표기)

---

## 4. 작업 전 필수 확인 및 보고 체계

작업을 시작하기 전, 추측하지 말고 **명시된 문서를 먼저 확인**하고 완료 후 반드시 업데이트하세요.

### 말하기 전에 돌릴 것 (Check First)
아래 주장을 하기 전 **왼쪽 명령어를 먼저 실행**하세요.

| 하려는 말 | 먼저 돌릴 것 |
| :-- | :-- |
| 시험장·RTSP 를 "안 했다 / 못 한다" | `sed -n "49,110p" docs/kisa/KISA_시험_체크리스트.md` |
| 안내서에 "그런 내용 없다" | `grep -n "<찾는말>" docs/kisa/안내서_원문발췌.md` |
| 학습셋·라벨을 "안 들어갔다 / 없다" | `python scripts/label_state.py` · `cat data/학습데이터/<셋>/meta.json` |
| 어떤 산출물이 "낡았다" | `python scripts/stale_scan.py` (시각 말고 내용을 본다) |
| 오검·미검 원인이 "무엇 때문이다" | `python scripts/why_fp.py` · `scripts/show_fire.py` · `scripts/zone_conf.py` |
| 다른 모델·문서의 원인을 인용할 때 | 우리 덤프로 재계산해 확인 (`scripts/fire_ens_try.py` · `early_late.py`) |

### 작업별 필수 확인 문서
| 하려는 일 | 확인해야 할 문서 |
| :--- | :--- |
| 파일 생성 / 경로·상수 작성 | `docs/file_path.md` (§1) |
| 결과·로그·가중치 저장 및 검색 | `docs/file_path.md` (§2) |
| 신규 데이터 수신 / 학습셋 추가 | `docs/data.md` |
| 실험(학습·채점) 큐 실행 | `docs/EXPERIMENTS.md` |
| 대시보드(`dash_v2`) 수정 | `docs/dashboard.md` |
| 성능 판단 및 수치 보고 | `docs/kisa/B200_평가.md` |
| 채점 규칙 및 F1 수식 확인 | `docs/kisa/평가.md` |
| 안내서 원문 확인 | `docs/kisa/안내서_원문발췌.md` |
| 시험장 절차 및 RTSP 준비 | `docs/kisa/KISA_시험_체크리스트.md` |
| 본시험 제출물(SA XML) 생성 | `docs/kisa/SA_생성_매뉴얼.md` |

---

## 5. 계측 요령 및 미검 원인 판별

### 규칙 및 계측 요령
점수가 안 오를 때 학습부터 다시 돌리지 마세요. **판정 규칙이 모델 교체보다 크게 움직인 적이 많습니다.**
* **추론은 한 번만:** 신뢰도를 덤프해 규칙은 파일로 훑습니다. (`scripts/fall_sweep.py`, `fire_rule_search.py`)
* **LOOCV/다른 덤프로 재검증:** 같은 채점셋으로 고르고 보고하면 낙관이 섞입니다.
* **`rc=-9` 에러:** SIGKILL이며 GPU 부족이 아닙니다. 고아로 남은 데이터로더 워커가 자원을 쥐고 있는지 확인하세요.

### 미검 원인 판별 가이드
* **프레임 한 장으로 판단 금지:** 구역 밖 다른 사람을 잡은 신뢰도일 수 있습니다.
* **구역 내 검출 표본 수 세기:** `scripts/fail_probe3.py`를 사용하여 판정이 요구하는 체류 시간을 채울 수 있는지 확인하세요.
* **축 추가 검토:** 상수 격자의 최고값이 현재값이면 "최적"이 아니라 "축이 모자라다"는 신호일 수 있습니다. (예: 4가지 조건을 동시에 걸어 해결)
* **증강(Augmentation) 열화 확인:** 증강을 만들기 전에, 그 증강이 만드는 열화가 실제 실패 케이스(소형/가림/저대비 등)와 일치하는지 먼저 측정하세요.

---

## 6. 배치·계보 자가 점검 스크립트

문서와 실제가 어긋났는지 눈으로 찾지 말고 아래 스크립트를 활용하세요. (어긴 것이 있으면 `1` 반환)
**실험 큐를 돌린 뒤, 또는 파일을 옮긴 뒤 한 번씩 실행하세요.**

| 명령 | 확인하는 것 |
| :--- | :--- |
| `.venv/bin/python scripts/check_layout.py --all` | 파일 위치가 `docs/file_path.md` 2절과 일치하는가 |
| `.venv/bin/python scripts/check_models.py` | 제출용 모델이 `results/MODELS.json`에 기록되었는가 |
| `python scripts/check_thor.py` | Thor 배포본이 서버와 같은 규칙·가중치인가 |
| `.venv/bin/python scripts/check_repro.py` | 오프라인 재현이 `results/BASELINE.json` 실측과 맞는가 |
| `.venv/bin/python scripts/fire_weight_check.py <실험> <가중치> [해상도]` | 덤프를 만든 가중치가 동일한 값을 내는가 |
| `.venv/bin/python scripts/audit_stale.py` | 중복 판정, 죽은 경로, 낡은 산출물, 미참조 스크립트 색출 |

*(※ 검사기를 새로 만들거나 고칠 때는 일부러 망가뜨린 것과 멀쩡한 것을 모두 테스트하는 `--selfcheck` 기능을 같이 만드세요.)*

---

## 7. 문서 편집 가이드라인 (이 문서를 고칠 때)
1. **절 제목은 명사구로 작성:** (O) "배치·계보 자가 점검" / (X) "헷갈리면 여기부터"
2. **제목에 날짜 금지:** 날짜는 본문에 적습니다.
3. **새 절 생성 자제:** 표에 행을 더할 수 있다면 기존 절을 활용하세요.
4. **덮어쓰기 금지:** 다른 세션과의 충돌 방지를 위해 기존 내용은 유지하고 덧붙이는 방식으로 수정하세요.